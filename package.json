{
  "name": "prepright",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "seed": "cd backend && python seed.py"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
